# Google Gemini API Configuration
# You can provide multiple API keys separated by commas for load balancing
# The system will randomly select one key from the pool for each request
# Examples:
#   Single key: GEMINI_API_KEYS=your_gemini_api_key_here
#   Multiple keys: GEMINI_API_KEYS=key1,key2,key3
GEMINI_API_KEYS=your_gemini_api_key_here

# Authentication Configuration
# IMPORTANT: Generate a secure random secret key for JWT tokens
# Generate with: openssl rand -hex 32
# Or with Python: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your_secret_key_here_min_32_characters_change_in_production

# Database Configuration
DATABASE_URL=sqlite:///./data/jewelry_designer.db

# Application Configuration
APP_NAME="PNJ Jewelry AI Designer"
APP_VERSION="1.0.0"
DEBUG=True
